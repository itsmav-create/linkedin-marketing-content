name: LinkedIn Content Digest

on:
  schedule:
    - cron: "0 20 * * 0"   # existing schedule (example: Sundays 20:00 UTC)
  workflow_dispatch: {}     # ← enables manual runs

jobs:
  run-li-digest:
    runs-on: ubuntu-latest
    ...
